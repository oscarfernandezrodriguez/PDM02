<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pdm02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fpedidos"></activity>
        <activity android:name=".Envio" />
        <activity
            android:name=".Compras"
            android:label="Ver compras realizadas">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Cliente" />
        </activity>
        <activity
            android:name=".Pedidos"
            android:label="Ver pedidos en trámite">

        </activity>
        <activity
            android:name=".Cliente"
            android:label="Logueado">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>